import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    gap: 16,
  },
  question: {
    fontSize: 22,
    textAlign: 'center',
    marginBottom: 8,
  },
});

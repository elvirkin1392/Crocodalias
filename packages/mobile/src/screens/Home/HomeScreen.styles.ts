import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  title: {
    paddingTop: 16,
    paddingHorizontal: 24,
    fontSize: 28,
    fontWeight: '600',
  },
});
